<div id="pins.transitions-enabled"></div>
<% @pins.each do |pin| %>
	<div class="box.panel.panel-default"></div>
	<%= link_to (image_tag pin.image.url(:medium)), pin %>
	<h2><%= link_to pin.title, pin %></h2>
	<p class="user">
		Submitted By
		<%= pin.user.email %>
	</p>
<% end %>